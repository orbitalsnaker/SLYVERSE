<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>SLYVERSE · 2026</title>
  <meta name="description" content="Hardware neural real. Inclusión total: diversidad funcional, idiomas, epilepsia-safe. Financiado por pervertidos ricos. Gratis para quien lo necesita.">
  
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&family=Roboto+Mono:wght@300;400;500&display=swap" rel="stylesheet">
  
  <style>
    :root {
      --blood: #ff0033;
      --ultra: #00ff99;
      --yellow: #ff0;
      --bg: #000;
      --muted: #0a0a0a;
      --text: #ddd;
      --glow: 0 0 60px;
      --high-contrast-bg: #000;
      --high-contrast-text: #fff;
    }
    * { margin:0; padding:0; box-sizing:border-box; }
    body { 
      background: var(--bg); 
      color: var(--text); 
      font-family: 'Roboto Mono', monospace; 
      overflow-x: hidden; 
      position: relative; 
      transition: background 0.3s, color 0.3s;
    }
    body.high-contrast { background: var(--high-contrast-bg); color: var(--high-contrast-text); }
    body.high-contrast * { color: var(--high-contrast-text) !important; }
    body.high-contrast h1, body.high-contrast h2 { color: #0f0 !important; text-shadow: none !important; }
    body.high-contrast .product, body.high-contrast .cta { background: #111; border-color: #0f0; }
    
    canvas { position:fixed; inset:0; z-index:-1; opacity:0.5; pointer-events:none; transition: opacity 0.6s; }
    body.no-matrix canvas { opacity: 0; }
    
    h1 {
      position: sticky;
      top: 0;
      background: var(--bg);
      padding: 3rem 0;
      font-family: 'Orbitron', sans-serif;
      font-size: clamp(4rem, 12vw, 11rem);
      letter-spacing: 14px;
      text-align: center;
      color: var(--blood);
      text-shadow: var(--glow) var(--blood);
      z-index: 10;
      animation: glitch 5s infinite;
      transition: text-shadow 0.4s;
    }
    body.no-glitch h1 { animation: none; text-shadow: none; }
    @keyframes glitch {
      0%,100% { text-shadow: var(--glow) var(--blood); }
      15% { text-shadow: -4px 0 #0ff, 4px 0 var(--blood); }
      30% { text-shadow: 4px 0 #0ff, -4px 0 var(--blood); }
      45% { text-shadow: -3px -3px #ff0, 3px 3px var(--blood); }
    }
    
    /* Panel de accesibilidad */
    #accessibility-panel {
      position: fixed;
      top: 20px;
      left: 20px;
      background: var(--muted);
      padding: 1.4rem 2rem;
      border: 3px solid var(--ultra);
      border-radius: 28px;
      z-index: 1000;
      font-size: 1.4rem;
      box-shadow: var(--glow) var(--ultra), 0 0 100px rgba(0,255,153,0.3);
      display: flex;
      flex-direction: column;
      gap: 1.2rem;
      backdrop-filter: blur(8px);
    }
    #accessibility-panel button, #accessibility-panel select {
      background: #000;
      color: var(--ultra);
      border: 2px solid var(--ultra);
      padding: 0.8rem;
      cursor: pointer;
      font-family: inherit;
      transition: all 0.3s;
    }
    #accessibility-panel button:hover { background: var(--ultra); color: #000; transform: scale(1.05); }
    
    /* Selector idioma */
    #lang-selector {
      position: fixed;
      top: 100px;
      right: 20px;
      background: var(--muted);
      padding: 1.4rem;
      border: 3px solid var(--ultra);
      border-radius: 28px;
      z-index: 999;
      backdrop-filter: blur(8px);
    }
    
    section { max-width: 1400px; margin: 0 auto; padding: 2rem 5vw; }
    .product, .cta {
      margin: 14rem auto;
      max-width: 1400px;
      background: var(--muted);
      padding: 8rem 7rem;
      border: 4px solid var(--ultra);
      border-radius: 40px;
      text-align: center;
      box-shadow: 0 0 120px rgba(0,255,153,0.35);
      backdrop-filter: blur(6px);
      transition: all 0.5s;
    }
    .product:hover, .cta:hover { 
      box-shadow: 0 0 180px rgba(0,255,153,0.6); 
      transform: translateY(-12px); 
    }
    
    .gallery { 
      display: grid; 
      grid-template-columns: repeat(auto-fit, minmax(420px, 1fr)); 
      gap: 40px; 
      margin: 5rem 0; 
    }
    .gallery img, .gallery video { 
      width: 100%; 
      border-radius: 24px; 
      border: 4px solid #333; 
      box-shadow: 0 0 60px #000; 
      object-fit: cover; 
      aspect-ratio: 4/3; 
      transition: all 0.5s;
    }
    .gallery img:hover, .gallery video:hover { 
      transform: scale(1.04); 
      box-shadow: 0 0 100px var(--ultra); 
      border-color: var(--ultra);
    }
    
    .price { 
      font-size: 2.6rem; 
      margin: 3rem 0; 
      color: var(--ultra); 
      text-shadow: 0 0 40px var(--ultra);
    }
    .status { font-size: 1.8rem; color: #bbb; margin: 2rem 0; }
    
    .cta h2 { 
      font-size: 4rem; 
      margin-bottom: 3rem; 
      color: var(--blood); 
      text-shadow: var(--glow) var(--blood); 
    }
    .cta p { 
      font-size: 2rem; 
      line-height: 2.2; 
      margin: 2.5rem 0;
    }
    .cta a {
      font-size: 2.6rem; 
      padding: 2.5rem 6rem; 
      background: var(--ultra); 
      color: #000; 
      border-radius: 99px; 
      text-decoration: none; 
      display: inline-block; 
      box-shadow: var(--glow) var(--ultra); 
      transition: all 0.5s;
    }
    .cta a:hover { 
      transform: scale(1.08); 
      box-shadow: 0 0 150px var(--ultra); 
    }
    
    footer { 
      padding: 12rem; 
      text-align: center; 
      color: #777; 
      font-size: 1.6rem; 
      text-shadow: 0 0 20px #777;
    }
    
    @media (max-width: 768px) {
      #accessibility-panel, #lang-selector { position: relative; left: auto; right: auto; margin: 3rem auto; width: 90%; }
      .product, .cta { padding: 5rem 3rem; margin: 10rem auto; }
      .gallery { grid-template-columns: 1fr; }
    }
  </style>
</head>
<body aria-label="Slyverse 2026 - Sitio accesible">

<canvas id="c" aria-hidden="true"></canvas>

<!-- Panel de accesibilidad -->
<div id="accessibility-panel" role="region" aria-label="Panel de accesibilidad">
  <label for="font-size">Tamaño texto:</label>
  <select id="font-size" onchange="changeFontSize(this.value)">
    <option value="1">Normal</option>
    <option value="1.5">Grande</option>
    <option value="2">Muy grande</option>
    <option value="3">Enorme</option>
  </select>
  
  <button onclick="toggleHighContrast()" id="contrast-btn">Alto contraste OFF</button>
  <button onclick="toggleMatrix()" id="matrix-btn">Matrix background ON</button>
  <button onclick="toggleGlitch()" id="glitch-btn">Glitch efectos ON</button>
  
  <button onclick="readPage()">Leer página en voz alta</button>
</div>

<!-- Selector idioma -->
<div id="lang-selector" role="region" aria-label="Selector de idioma">
  <label for="language">Idioma:</label>
  <select id="language" onchange="changeLanguage(this.value)">
    <option value="es">Español</option>
    <option value="en">English</option>
    <option value="pt">Português</option>
    <option value="fr">Français</option>
    <option value="de">Deutsch</option>
    <option value="it">Italiano</option>
    <option value="ru">Русский</option>
    <option value="zh">中文</option>
    <option value="ja">日本語</option>
    <option value="ar">العربية</option>
    <option value="hi">हिन्दी</option>
    <option value="tr">Türkçe</option>
  </select>
</div>

<h1>Slyverse · 2026</h1>

<section>
  <p id="tagline" style="text-align:center;font-size:3rem;max-width:1200px;margin:7rem auto;line-height:2;">
    Hardware real para kink, disforia y euphoria extrema · Inclusión total para todas las diversidades funcionales<br>
    Harm reduction para fiesteros · Financiado por pervertidos ricos · Gratis eterno para quien lo necesita.
  </p>
</section>

<!-- Producto principal mejorado con ideas open-source (kitsune/oni aesthetic sin coste extra) -->
<div class="product">
  <h2>DANCE EXO-ADAPT MK-1</h2>
  <p style="font-size:2rem;line-height:2.2;max-width:1100px;margin:0 auto 4rem;">
    Exoesqueleto modular ligero 100% open-hardware adaptable a cualquier diversidad funcional: silla de ruedas, amputaciones, parálisis...<br>
    Control por EMG, IMU, sip&puff, eye-tracking, voz o pulso.<br>
    Para bailar, terapia de movimiento o euphoria corporal total.<br><br>
    <strong>Nuevo en 2026:</strong> Accesorios estéticos kitsune/oni open-source (colas articulares imprimibles, orejas sensorizadas, máscaras LED minimalistas) integrados sin subir coste — todo BOM bajo 4000 € o gratis si lo construyes tú.
  </p>
  <div class="price">3 999 € · edición personalizada<br><small>(o GRATIS eterno si lo necesitas y lo construyes tú · BOM + STL open-source)</small></div>
  <div class="status">DESARROLLO AVANZADO · Lanzamiento previsto 2026 · Accesorios kitsune/oni incluidos</div>
  
  <div class="gallery">
    <img src="https://example.com/exo1.jpg" alt="Persona en silla de ruedas con exoesqueleto Slyverse y cola kitsune bailando en rave cyberpunk" loading="lazy">
    <img src="https://example.com/exo2.jpg" alt="Detalle de orejas kitsune sensorizadas y control ocular en exoesqueleto" loading="lazy">
    <video autoplay loop muted playsinline aria-label="Demostración de movimiento inclusivo con accesorios kitsune en DANCE EXO-ADAPT">
      <source src="https://example.com/exo-demo.mp4" type="video/mp4">
    </video>
  </div>
</div>

<!-- CTA -->
<section class="cta">
  <h2>Esto está pasando de verdad.</h2>
  <p style="font-size:2.2rem; margin: 3rem 0; line-height:2;">
    La waitlist y cualquier tipo de pre-venta o ingreso de dinero <strong>no comenzará hasta mediados de 2026</strong>.<br><br>
    Preferimos asegurarnos al 100% de la viabilidad técnica, seguridad y perfección antes de aceptar un solo céntimo.
  </p>
  <p style="font-size:2rem; margin: 4rem 0; color:var(--ultra); line-height:2;">
    Todos los BOM, STL, esquemas y diseños (incluyendo accesorios kitsune/oni open-source) son y seguirán siendo <strong>100% open-hardware</strong>.<br>
    Si realmente lo necesitas o quieres colaborar de verdad, escríbenos.
  </p>
  <p style="margin-top: 5rem;">
    <a href="https://x.com/0rb1t4lsn4k3r" style="padding:2.5rem 6rem; box-shadow: var(--glow) var(--ultra);">
      Contacto solo por DM en X: @0rb1t4lsn4k3r
    </a>
  </p>
</section>

<footer>
  Slyverse · Shenzhen & España · 2025-2026<br>
  Inclusión real · Hardware para todos los cuerpos · Sin barreras · Kitsune/oni aesthetics open-source<br>
  DM en X: @0rb1t4lsn4k3r
</footer>

<script>
  // Matrix background con toque japonés sutil
  const canvas = document.getElementById('c');
  const ctx = canvas.getContext('2d');
  let w, h;
  function resize() { w = canvas.width = innerWidth; h = canvas.height = innerHeight; }
  resize();
  addEventListener('resize', resize);
  
  const chars = '01キツネ鬼舞適応スリバース狐妖';
  const size = 20;
  let cols = Math.floor(w / size);
  let drops = new Array(cols).fill(1);
  
  function draw() {
    if (document.body.classList.contains('no-matrix')) return;
    ctx.fillStyle = 'rgba(0,0,0,0.07)';
    ctx.fillRect(0, 0, w, h);
    ctx.fillStyle = '#0ff';
    ctx.font = `${size}px monospace`;
    for (let i = 0; i < drops.length; i++) {
      const text = chars[Math.floor(Math.random() * chars.length)];
      ctx.fillText(text, i * size, drops[i] * size);
      if (drops[i] * size > h && Math.random() > 0.975) drops[i] = 0;
      drops[i]++;
    }
    requestAnimationFrame(draw);
  }
  draw();
  
  // Accesibilidad
  function toggleHighContrast() {
    document.body.classList.toggle('high-contrast');
    document.getElementById('contrast-btn').textContent = document.body.classList.contains('high-contrast') ? 'Alto contraste ON' : 'Alto contraste OFF';
  }
  
  function toggleMatrix() {
    document.body.classList.toggle('no-matrix');
    document.getElementById('matrix-btn').textContent = document.body.classList.contains('no-matrix') ? 'Matrix background OFF' : 'Matrix background ON';
  }
  
  function toggleGlitch() {
    document.body.classList.toggle('no-glitch');
    document.getElementById('glitch-btn').textContent = document.body.classList.contains('no-glitch') ? 'Glitch efectos OFF' : 'Glitch efectos ON';
  }
  
  function changeFontSize(multiplier) {
    document.body.style.fontSize = `${multiplier}rem`;
  }
  
  function readPage() {
    if ('speechSynthesis' in window) {
      const text = document.body.innerText;
      const utterance = new SpeechSynthesisUtterance(text);
      utterance.lang = document.documentElement.lang || 'es';
      utterance.rate = 0.9;
      speechSynthesis.speak(utterance);
    }
  }
  
  function changeLanguage(lang) {
    document.documentElement.lang = lang;
    document.documentElement.dir = (lang === 'ar') ? 'rtl' : 'ltr';
    localStorage.setItem('slyverse-lang', lang);
  }
  
  window.onload = () => {
    if (localStorage.getItem('high-contrast') === 'true') toggleHighContrast();
    if (localStorage.getItem('no-matrix') === 'true') toggleMatrix();
    if (localStorage.getItem('no-glitch') === 'true') toggleGlitch();
    changeLanguage(localStorage.getItem('slyverse-lang') || 'es');
  };
</script>

<script type="module" src="https://unpkg.com/@google/model-viewer/dist/model-viewer.min.js"></script>

</body>
</html>