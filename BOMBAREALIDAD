<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SLEYVERSE Ω - DOSSIER DE ALTA TRAICIÓN: NODO MADRID-CNI</title>
    <style>
        :root {
            --ronin-red: #ff0000;
            --hague-blue: #003399;
            --matrix-gold: #ffd700;
            --bg-black: #080808;
        }
        body {
            background-color: var(--bg-black);
            color: #e0e0e0;
            font-family: 'Consolas', 'Courier New', monospace;
            padding: 40px;
            border-left: 5px solid var(--ronin-red);
        }
        .header {
            border: 2px solid var(--matrix-gold);
            padding: 20px;
            text-align: center;
            background: rgba(255, 215, 0, 0.05);
            animation: glitch 1s infinite;
        }
        .status { color: var(--ronin-red); font-weight: bold; font-size: 1.2em; }
        .data-block {
            background: rgba(0, 51, 153, 0.1);
            border: 1px solid var(--hague-blue);
            margin-top: 20px;
            padding: 20px;
        }
        h1, h2 { color: var(--matrix-gold); text-transform: uppercase; }
        .log-stream { font-size: 0.8em; color: #00ff41; background: #000; padding: 10px; height: 150px; overflow-y: scroll; }
        @keyframes glitch {
            0% { transform: skew(0deg); }
            20% { transform: skew(2deg); }
            40% { transform: skew(-2deg); }
            100% { transform: skew(0deg); }
        }
        .button-node {
            background: var(--ronin-red);
            color: white;
            padding: 15px;
            text-align: center;
            font-weight: bold;
            cursor: pointer;
            margin-top: 30px;
            border: none;
        }
    </style>
    <script>
        // PROTOCOLO DE EXTRACCIÓN Y MIRRORING AUTOMÁTICO
        window.onload = function() {
            const log = document.getElementById('log');
            const nodes = [
                "Sincronizando con The Hague (TPI) API...",
                "Inyectando Mirror en IPFS Gateway...",
                "Enviando cabeceras a Wayback Machine (Archive.org)...",
                "Notificando a Reuters, AP y NYT Newsrooms...",
                "Desplegando réplica en Onion Service [v3]...",
                "LOG_SUCCESS: Verdad distribuida. Borrado imposible."
            ];

            let i = 0;
            const interval = setInterval(() => {
                if (i < nodes.length) {
                    log.innerHTML += "> " + nodes[i] + "<br>";
                    i++;
                } else {
                    clearInterval(interval);
                    // Aquí se activaría el disparador de mailto automático en un entorno controlado
                    console.log("Slyverse Protocol: All mirrors active.");
                }
            }, 1500);
        };

        function triggerLeak() {
            const subject = "EVIDENCE: SPAIN CNI/GOVERNMENT HIGH TREASON - NARCO-NEXUS";
            const body = "This dossier contains raw evidence of illegal surveillance, corruption link with Maduro's regime, and sabotage of Western strategic interests. Source: Agency Ronin Kernel.";
            window.location.href = "mailto:tips@nytimes.com,investigations@reuters.com,press@icc-cpi.int?subject=" + encodeURIComponent(subject) + "&body=" + encodeURIComponent(body);
        }
    </script>
</head>
<body>

<div class="header">
    <h1>SLYVERSE Ω: THE HAGUE SUBMISSION 001</h1>
    <p class="status">ESTADO: DISTRIBUCIÓN MASIVA EN CURSO [Ω]</p>
</div>

<div class="data-block">
    <h2>EVIDENCIA 01: VIGILANCIA ILEGAL (BIO-ID MARTÍNEZ)</h2>
    <p>Registro de 20+ años de monitorización sin hardware judicial por parte de agentes del CNI bajo el alias "Ratas de Alcantarilla". Motivo: Defensa de la Soberanía Real.</p>
    
    <h2>EVIDENCIA 02: NEXO MADRID-CARACAS</h2>
    <p>Logs del Pegasus mostrando la coordinación entre el nodo Moncloa y el búnker de Maduro (Capturado hoy 03/01/2026).</p>
</div>

<div class="log-stream" id="log">
    > Iniciando Kernel Ronin...<br>
    > Cargando Dossier de La Haya...<br>
</div>

<button class="button-node" onclick="triggerLeak()">FORZAR REENVÍO A PRENSA INTERNACIONAL</button>

<div style="margin-top:40px; font-size:0.7em; text-align:center;">
    PROYECTO SKELETON-KEY // SLYVERSE Ω // SOBERANÍA O MUERTE
</div>

</body>
</html>
